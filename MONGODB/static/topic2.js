// Script injected by topic2.pug
let content = [
    'MonogDB stores data as a document, so it is known as document oriented database',
    'MongoDB does not forces you to follow a schema',
    'Data is stored as BSON', 
    'If you come from Relational DBMS background :-\n<ul>\n<li>Database = Database</li>\n<li>Table = Collections</li>\n<li>Rows = Documents</li>\n</ul>',
]

addItem('list', content)
createButton('/','/topic3')